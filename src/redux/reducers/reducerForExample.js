const defaultState = {
  name: 'jesus Gabriel',
  lastName: 'Rivera',
  emailAddress: 'jgrcreations@gmail.com'
}

const exampleState = (state = defaultState, action) => {
  switch(action.type) {
    default: return state
  }
}
export default exampleState
